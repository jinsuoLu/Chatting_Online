import { Controller, Get, Post, Patch, Body, Param, Req, UnauthorizedException } from '@nestjs/common'; import { QuotasService } from './quotas.service.js';
@Controller('quotas') export class QuotasController { constructor(private readonly q:QuotasService){} private a(r:any){if(!r.user) throw new UnauthorizedException();return r.user} @Post('admins') create(@Req()r:any,@Body()b:any){return this.q.createAdmin(this.a(r),b)} @Patch('admins/:id') update(@Req()r:any,@Param('id')id:string,@Body()b:any){return this.q.update(this.a(r),id,b)} @Get('admins') list(@Req()r:any){return this.q.list(this.a(r))} }
