'use client';import{useState}from'react';import{useRouter}from'next/navigation';import{api}from'../admin/ui';export default function Login(){const[u,su]=useState(''),[p,sp]=useState(''),[e,se]=useState(''),r=useRouter();return <main className="login"><h1>管理员登录</h1><form onSubmit={async x=>{x.preventDefault();try{await api('/auth/login',{method:'POST',body:JSON.stringify({username:u,password:p})});r.push('/admin/dashboard')}catch(x:any){se(x.message)}}}><input placeholder="用户名" value={u} onChange={x=>su(x.target.value)} required/><input placeholder="密码" type="password" value={p} onChange={x=>sp(x.target.value)} required/>{e&&<p className="error">{e}</p>}<button>登录</button></form></main>}
