'use client';import{useState}from'react';import{api,Shell}from'../../ui';export default function P(){const[p,sp]=useState('Room'),[c,sc]=useState(1),[r,sr]=useState<any>();return <Shell title="批量创建"><form className="panel login" onSubmit={async x=>{x.preventDefault();sr(await api('/rooms/batch',{method:'POST',body:JSON.stringify({namePrefix:p,count:c})}))}}><input value={p} onChange={x=>sp(x.target.value)} placeholder="名称前缀"/><input type="number" min="1" value={c} onChange={x=>sc(+x.target.value)}/><button>开始创建</button>{r&&<p>成功 {r.succeeded} / {r.requested}</p>}</form></Shell>}
