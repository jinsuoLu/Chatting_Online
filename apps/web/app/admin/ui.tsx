'use client';
export async function api(path:string,init:RequestInit={}){const r=await fetch('/api/v1'+path,{...init,credentials:'include',headers:{'Content-Type':'application/json',...(init.headers||{})}});if(r.status===401){location.href='/login';throw Error('AUTH_REQUIRED')}if(r.status===403)throw Error('FORBIDDEN');if(!r.ok)throw Error('REQUEST_FAILED');return r.json()}
export function Shell({title,children}:{title:string;children:React.ReactNode}){return <div className="shell"><aside><b>Chatting Online</b><nav><a href="/admin/dashboard">概览</a><a href="/admin/rooms">聊天室</a><a href="/admin/access-links">访问链接</a><a href="/admin/audit-logs">审计日志</a><a href="/super-admin/admins">超级管理员</a></nav></aside><main className="content"><h1>{title}</h1>{children}</main></div>}
export const State=({loading,error,empty}:{loading?:boolean;error?:string;empty?:boolean})=><div className="state">{loading?'加载中…':error?(error==='FORBIDDEN'?'无权限访问':'请求失败'):empty?'暂无数据':''}</div>;
